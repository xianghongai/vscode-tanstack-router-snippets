{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Preview Snippets",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "preLaunchTask": "Build snippets"
    }
  ]
}
